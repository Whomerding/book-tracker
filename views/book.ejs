<body>

    <H1>
        <%= book.title %>
    </H1>
    <p>Author: <%= book.author %>
    </p>
    <p>Rating: <%= book.official_rating %>
    </p>
    <img src="<%= book.cover %>" alt="<%= book.title %>">
    </div>
    <% if (book.personal_rating) { %>
        <p>Your Rating: <%= book.personal_rating %>
        </p>
        <% } else { %>
            <form action="/update/<%= book.id %>" method="post">
                <label for="personal_rating">Your Rating (1-5):</label>
                <input type="number" id="personal_rating" name="personal_rating" min="1" max="5" step="0.1"
                    value="<%= book.personal_rating %>">
                <button type="submit">Save Rating</button>
            </form>
            <% } %>
                <h2>Your Summary</h2>



                <% if (book.summary) { %>
                    <p>
                        <%= book.summary %>
                    </p>
                    <% } else { %>
                        <form action="/update/<%= book.id %>" method="post">

                            <label for="summary">Your Summary:</label><br>
                            <textarea id="summary" name="summary" rows="4" cols="50"><%= book.summary %></textarea><br>
                            <button type="submit">Save Summary</button>
                        </form>
                        <% } %>
                            <h2>Your Notes</h2>
                            <% notes.forEach(note=> { %>
                                <p>
                                    <%= note.note %>
                                </p>
                                <% }); %>

                                    <form action="/update/<%= book.id %>" method="post">
                                        <label for="note">Your Notes:</label><br>
                                        <textarea id="note" name="note" rows="4" cols="50"></textarea><br>
                                        <button type="submit">Save Note</button>
                                    </form>



</body>

<!-- Life changing wisdom about the perception of ourselves and the world.  Don't believe the lies that others have told you about yourself and the world as a whole.  Questions everything and grow! -->